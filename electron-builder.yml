appId: com.vdosamurai.app
productName: VDO Samurai
copyright: Copyright © 2024 VDO Samurai

directories:
  buildResources: build
  output: dist

files:
  - out/**/*
  - "!**/*.{iml,o,hprof,orig,pyc,pyo,rbc,swp,csproj,sln,xproj}"
  - "!.editorconfig"
  - "!**/._*"
  - "!**/{.DS_Store,.git,.hg,.svn,CVS,RCS,SCCS,.gitignore,.gitattributes}"
  - "!**/{thumbs.db,Thumbs.db,desktop.ini,.eslintrc,.eslintrc.js,.eslintignore}"
  - "!**/{node_modules,src,public,electron}"
  - "!**/*.{ts,tsx,map,md}"
  - "!{.prettierrc,.prettierignore,tsconfig*.json,vite.config.ts,electron.vite.config.ts}"

asarUnpack:
  - resources/ffmpeg/**/*

mac:
  extraResources:
    - from: resources/ffmpeg/ffmpeg-darwin-${arch}
      to: ffmpeg/ffmpeg
    - from: resources/ffmpeg/ffprobe-darwin-${arch}
      to: ffmpeg/ffprobe
  category: public.app-category.video
  target:
    - target: dmg
      arch:
        - x64
        - arm64
    - target: zip
      arch:
        - x64
        - arm64
  icon: build/icon.icns
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  darkModeSupport: true

dmg:
  contents:
    - x: 130
      y: 220
    - x: 410
      y: 220
      type: link
      path: /Applications
  window:
    width: 540
    height: 380

win:
  extraResources:
    - from: resources/ffmpeg/ffmpeg.exe
      to: ffmpeg/ffmpeg.exe
    - from: resources/ffmpeg/ffprobe.exe
      to: ffmpeg/ffprobe.exe
  target:
    - target: nsis
      arch:
        - x64
    - target: portable
      arch:
        - x64
  icon: build/icon.ico

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  deleteAppDataOnUninstall: true
  createDesktopShortcut: true
  createStartMenuShortcut: true

linux:
  extraResources:
    - from: resources/ffmpeg/ffmpeg-linux-x64
      to: ffmpeg/ffmpeg
    - from: resources/ffmpeg/ffprobe-linux-x64
      to: ffmpeg/ffprobe
  target:
    - target: AppImage
      arch:
        - x64
    - target: deb
      arch:
        - x64
  category: Video
  icon: build/icons
  maintainer: VDO Samurai <noreply@vdosamurai.invalid>

publish:
  provider: github
  releaseType: draft
